import { Hostbinding2propertyHighlightDirective } from './hostbinding2property-highlight.directive';

describe('Hostbinding2propertyHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new Hostbinding2propertyHighlightDirective();
    expect(directive).toBeTruthy();
  });
});

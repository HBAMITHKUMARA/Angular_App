import { HostlistenerHighlightDirective } from './hostlistener-highlight.directive';

describe('HostlistenerHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new HostlistenerHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
